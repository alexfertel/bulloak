Disambiguation
├── when a is even
│  ├── given zero
│  │  └── it should revert
│  └── given not zero
│     └── it should work
└── when b is even
    ├── given zero
    │   └── it should revert
    └── given not zero
        └── it should work

